import { ScreenShell } from "@/components/screen-shell";

export default function QuickAddModalScreen() {
  return (
    <ScreenShell
      title="Quick Add"
      description="Use this modal flow to quickly add a medication, schedule entry, or reminder."
    />
  );
}
