import { ScreenShell } from "@/components/screen-shell";

export default function SendReminderScreen() {
  return (
    <ScreenShell
      title="Send Reminder"
      description="Trigger an immediate reminder notification for the next pending dose."
    />
  );
}
